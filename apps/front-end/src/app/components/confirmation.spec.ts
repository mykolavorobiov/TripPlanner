import { Confirmation } from './confirmation';

describe('Confirmation', () => {
  it('should create an instance', () => {
    const directive = new Confirmation();
    expect(directive).toBeTruthy();
  });
});
